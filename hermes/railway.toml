[build]
builder = "dockerfile"
dockerfilePath = "hermes/Dockerfile"

[deploy]
startCommand = "node dist/hermes/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
numReplicas = 1
